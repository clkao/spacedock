---
session-date: 2026-05-30
sequence: 2
first-commit: 74b6d44
last-commit: e65d18f
duration: ~3h
main-repo-range: e13cc19..0f9d628
---

# Session Debrief — 2026-05-30 #2

Autonomous sprint, session 2: shipped the **self-hosting core** (native `spacedock dispatch` joins native `spacedock status`; the dispatch path is now Python-free) plus the `--json` agent-output flywheel, ran an architecture review that **blessed the core (no rework)**, and re-scoped the sprint around a set of captain-driven engineering principles. Three entities landed (all direct merges, no PR flow); four new entities filed; packaging held one step short of merge for a deeper dive next session.

## Shipped
- **external-tracker-checkpoint** `external-tracker-checkpoint` — direct merge `2ff31d1`; PASSED. The deliverable was a decision + a preserve-through-lifecycle contract; folded the one un-fixtured AC (folder-form `issue`/`source` survives report-append) into the sibling suites. (Became the live cautionary tale for the doc-only / self-oracle antipattern: its AC-6 was a prose self-oracle.)
- **agent-output-modes** `agent-output-modes` — direct merge `87b4735`; PASSED. The flywheel: opt-in `--json`/`--fields`/`--quiet` for agent-frugal status reads (all-string envelopes, fixed key order, `SetEscapeHTML(false)`); the FO's own Event-Loop reads switched to `--json`. Cycle-1 staff audit caught 8 holes; cycle-2 fix closed them (incl. the `--validate --json` false-path the validator missed).
- **native-dispatch-helper** `native-dispatch-helper` — direct merge `0f9d628`; PASSED. The Python-killer: native `spacedock dispatch build` + `show-stage-def` in a new `internal/dispatch` pkg, semantic-parity with the vendored Python oracle (one fetch line rewritten to `spacedock dispatch`, 18 byte-identical + 2 str(e)-prefix errors). The parallel staff audit caught 3 oracle divergences + the abs-worktree `filepath.Join` blocker the validator's range missed; all fixed + re-validated.

## Filed (backlog)
- **zs** `claude-runtime-segregation` — owns the runtime-coupled `claude-team` surface (context-budget + standing subcommands) + the Claude-vs-generic operating-contract split; ideation-APPROVED (spiked: context-budget native parity is cheap; the host-neutral seam is a capability boundary, not a per-host `if`). Folds in the context-budget 1M-detection fix.
- **hx** `deliverable-contract-hardening` — encodes the four deliverable principles + portability + FO-posture + portable-TDD + ergonomics into the shipped contract, with a `status --validate` self-reference check + terminal-PASSED guard; ideation-APPROVED (spiked the lint to precision/recall 1.0 on all 79 real ACs).
- **zj** `yaml-parser-migration` — post-oracle migration of the frontmatter parser/mutator to a YAML library (yaml.Node round-trip), deliberate documented divergences; gated on Python-oracle retirement; ~200–350 net LOC removal estimated.
- **0x** `architecture-review-cleanups` — the architecture review's now-safe must-fixes (export `pyJoin`; parity tests fail-not-skip when the oracle is absent) + verified dead-code removals.

## Non-PR commits (workflow-only)
No PR flow in this bootstrap workflow — everything landed via direct `--no-ff` merges on main; entity state lives in the `.spacedock-state` repo. Surfaced workflow-only commits:
- Feedback cycles: `157f17d` (packaging ideation cycle-1 reject), `7f16369` (native-dispatch-helper validation→implementation reflow, cycle 2). Agent-output-modes + native-dispatch-helper ideation rejects also routed (cycle-1/cycle-2).
- Principle/decision notes: `3ae57ac` (Go-binary dependency policy), `1528e03` (parity-with-Python is a migration scaffold), `f6eaca0` (two-origin / distribution-remotes), `5a43dfa`+`34485d1` (roborev descope→ideation-spike), `e65d18f` (packaging validation HELD).
- Architecture review report written to `docs/dev/_reviews/architecture-review.md` (untracked); design proposals in `docs/dev/_proposals/` (untracked).

## Decisions
- **Concurrency override to 3** per stage — enabled fanning out ideation (collision-free) while implementation serialized on shared `internal/status`/`internal/cli`/contract surfaces.
- **Deliverable principles** (captain, organic from the external-tracker AC-6 + TDD discussions): (P1) no doc-only deliverable — proof must be EXTERNAL to the entity and able to FAIL; (P2) proof is behavioral, not grep (grep-over-code = spelling; grep-over-prose worse); (P3) enforce in code, not prose; (P4) spike the riskiest unknown IN ideation. Plus **PORTABILITY / self-sufficiency** (the shipped contract assumes nothing from the user's env) and the **FO-posture trio** (begin-with-the-end / yes-able-ask / JFDI).
- **TDD lives in the portable `spacedock:ensign` contract, NOT global CLAUDE.md** — a clean-room self-hosted session has no CL CLAUDE.md (corrected the comparison workflow's "rely on global" conclusion).
- **Plain language, no "oracle" jargon** in the shipped contract; the FO contract carries only the gate-check.
- **native-dispatch-helper:** parity north-star = semantic-equivalent + rewrite fetch lines (NOT byte-identical-to-Python); export-in-place seam (not leaf-lift); dispatch-first sequencing; the `_mods`/standing surface + context-budget scope-split to zs.
- **agent-output-modes:** table `--fields` de-dupes already-displayed columns (fixes a latent dup-column bug).
- **Go-binary dependency policy:** zero-dep was a Python-shipped-with-skill artifact; the compiled binary may PREFER libraries — hand-roll only for oracle-parity (bootstrap) or byte-preservation. Parity-with-Python is a migration scaffold, not a long-term goal.
- **roborev:** descoped from the BUILD (sandbox-blocked + a dev-workflow improvement, not core); operating-model decided = **#2 PR-MOD** (the daemon/ALONGSIDE path is OS-denied; `ci review` is daemonless); build deferred behind the post-bootstrap pr-merge mod + a clean-auth review host.
- **`--quiet` kept** (architecture review recommended pruning as unused; captain elected to keep).
- **Architecture review:** dispatched on the merged core; verdict — sound, no rework. **packaging HELD** at validation for a deeper dive next session.
- **yaml-parser-migration filed** with prerequisite + LOC estimate.

## Issues — Workflow
- native-dispatch-helper: abs-worktree `filepath.Join` doubled absolute worktree paths (the FO sets absolute `worktree:` values) — a real self-hosting blocker; fixed (`pyJoin`) + a parity fixture; parity suite had only stamped relative worktrees.
- agent-output-modes: AC-4 test under-asserted (`Contains` vs the two duplicate `--next` reads); `--validate --json` never emitted the false-path envelope — both fixed cycle-2.
- spacedock-packaging (HELD, next session): (1) front-door gate hole — launches on a non-empty-but-missing manifest (`gateHost` must reject the `NoPluginFound` verdict); (2) codex resolver shells `codex plugin list --json`, rejected by real codex 0.132.0. Full detail in `_sprint-notes.md`.

## Issues — Spacedock
- **context-budget 1M-detection bug** (the `claude-team` helper): `context_limit_for_model` returns 200k for `claude-opus-4-8` ensigns (the EXTENDED list has 4-7 not 4-8, and spawned ensigns drop the `[1m]` suffix), so `reuse_ok` false-negated every ensign all session at the wrong denominator — fix is a forward family rule (opus minor ≥ 7 ⇒ 1M), folded into `zs`. NOT filed (need approval per CL's policy; being fixed in zs).
- **rtk mangles `git log` text output** (and summarizes `go test`) — confirmed again this session (the `git log` reads returned truncated/wrong commits; `merge-base --is-ancestor` exit-code checks were reliable). The `--json` flywheel addresses status reads but not `git`/`go` output. NOT filed (rtk is an external proxy, not spacedock).

## Observations
- **Dual-review (validator + parallel staff audit) on the high-stakes entities earned its keep repeatedly** — the audit caught what the validator missed every time: the `--validate --json` gap (agent-output-modes), the 3 oracle divergences (native-dispatch-helper), the front-door gate hole + codex resolver (packaging). For parity/contract/flip work, the parallel audit is non-optional.
- **Spike-in-ideation caught the unknown-unknowns** that depth-first work would only surface after hours: `--boot` non-determinism, the show-standing parity-vs-AC2 conflict, the cross-repo manifest reality, that HOME-relocation breaks roborev's claude-code auth, the abs-worktree bug on the live entity. Every ideation that spiked arrived at its gate with the riskiest claim demonstrated.
- **The principles emerged from real failures**, not theory: external-tracker's AC-6 self-oracle (→ no-doc-only + behavioral-proof), the TDD-via-global-CLAUDE.md slip (→ portability), the context-budget 200k false-negative (→ trust the artifact, not a stale model list).
- **Ideation parallelizes; implementation serializes** on the shared Go/contract surfaces — the sprint's natural shape once concurrency was 3.

## What's Next
**Resume (priority):**
1. **spacedock-packaging** — route the 2 audit fixes (front-door gate hole + codex resolver), re-validate (staff-audited), merge → completes the distribution wrapper. Detail in `_sprint-notes.md`.
2. **Implementation drain** (serialized on shared surfaces): **zs** (runtime segregation + context-budget native + the 1M fix + scope-the-Python-free-claim, review must-fix #4) → **cli-ergonomics** (auto-discovery + actionable errors) → **hx** (the principles refit + the self-reference guard) → **0x architecture-review-cleanups** (pyJoin export, parity fail-not-skip, dead-code).

**Deferred / post-bootstrap (sprint-notes):**
- **zj yaml-parser-migration** — once the Python oracle retires (post-parity-certification + zs lands).
- **roborev build** — once the pr-merge mod exists + a clean-auth review host.
- Two-origin model + state orphan-branch origin + pr-mod coordination; VendorRunner retirement; AGENTS.md dep-policy revision.

**Captain repo actions:** add a remote to `~/git/spacedock` + push `next` (for packaging's `@next` install verification); give `.spacedock-state` an orphan-branch origin.

**Cleanup:** a detached audit checkout `.worktrees/audit-spacedock-packaging` is removable; the packaging worktree/branch persist (resume there).
