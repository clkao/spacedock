---
session-date: 2026-06-01
sequence: 1
first-commit: gate-feedback-loop seed (post 2026-05-31-01 debrief)
last-commit: 4c3d380
duration: ~8h
---

# Session Debrief — 2026-06-01 #1

Behavior-coverage sprint that grew into shipping the live-runtime CI net and standing up the split-root collaboration infra. Headline: closed the deferred "live half" of the coverage matrix (real claude/codex driving the FO→ensign cycle, green in approval-gated GitHub Actions), and operationalized state-root (state-remote applied, pr-mod ideated). The adversarial-audit-on-detached-checkout discipline caught real bugs the validators missed every time it ran.

## Shipped (landed on `next`)
- **m6** `gate-feedback-loop-behavior-coverage` — row-15 behavioral coverage: the `dispatch.Run` feedback-reflow seam in `internal/ensigncycle`, anchored emit-form assertions, both negatives independently reproduced. PASSED.
- **mv** `harden-integration-prose-grep-tests` — retired 7 prose-grep assertions (coverage owned by named seams) + repointed `dispatch_test.go` at native `dispatch.Run`; coverage-equivalence independently verified. PASSED.
- **k6** `dispatch-nonworktree-state-guidance` — [#238]. Split-root state-commit guidance on both branches with the *resolved* state checkout (`workflowDir/<state>`). 1 feedback cycle: a state-checkout-binding bug (bound `workflowDir` not the state dir) caught by a parallel adversarial audit, fixed, re-validated clean.
- **5b** `split-root-ergonomics-and-remote-model` (Phase A) — [#239]. A0 absolutize `workflowDir` in `runBuild` (cwd-independent emitted path); A1 surface `state_backend`/`definition_dir`/`entity_dir`/`entity_dir_present` in `status --boot --json`; A2 commission split-vs-single decision rule; A3 single-root no-guidance negative test. Validator + adversarial audit clean.
- **b6** `live-e2e-ci-footing` — [#237]. Approval-gated live-runtime e2e: `runtime-live-e2e.yml` (workflow_dispatch + label-gated `pull_request: branches:[next]`) + `//go:build live` `TestLiveEnsignCycle` shelling the real `spacedock claude` front door. **Proven green in real CI**: offline + sonnet (CI-E2E) + opus (CI-E2E-OPUS), both models drove the full FO→ensign→done→archive cycle. Security-audited clean. 5 feedback/refinement cycles (auth, archive-aware end-state, sonnet+opus matrix, verdict-set, heading-regex, PR-trigger).

## Filed (backlog)
- **sc** `split-root-collaboration-remote-resume` (Phase B) — same-repo orphan-branch state model + `$inline` sentinel + workflow-slug state-branch + `spacedock state init` resume + FO sanity-check + push/`pull --rebase` sync. Ideation gate-ready (orphan-branch model captain-approved; 3 mechanics spiked).
- **0m** `pr-merge-mod` — first merge-lifecycle mod: opens the code-branch PR to `next` at terminalization, state-root-aware. Ideation gate-ready (lives in `.spacedock-state/_mods/` — probe-proven correction).
- **2a** `require-external-proof-guard` (hx-A) — refuse to close a task whose only proof re-reads itself; carries staff fixes M1–M4.
- **se** `ship-working-principles-in-contract` (hx-B) — encode the principles + FO posture + test-first into the shipped instructions.
- **5t** `no-hidden-machine-dependencies` (hx-C) — portability test scoped to `skills/` (staff fix M5).

## Non-PR commits (workflow-only)
- Superseded **hx** `deliverable-contract-hardening` → decomposed into A/B/C after a staff review found it too broad and the guard design needing rework (M1–M5). Archived as the design parent.
- Re-ideated **ng** `roborev-validation-hook`: sandbox-auth blocker disproven (roborev's nested claude-code agent authed + caught a seeded SQLi); recommendation flipped to VALIDATION-PASS primary + PR-JOB follow-on, multi-agent fanout+synthesis made first-class (AC-4).
- Applied the **state-remote**: `docs/dev/.spacedock-state` now has `origin` = main repo, `spacedock-state/dev` pushed (`4c3d380`).

## Decisions
_(captain to add)_ — Notable forks the captain made: keep `main` (it's the released install path) → no next→main promotion, scope main's stale `pull_request_target` to `branches:[main]`; same-repo orphan-branch state model (not separate-repo); sonnet is the model floor (haiku dropped); relax verdict assertion; relax sandbox for roborev; decompose hx; live-on-PR via `pull_request` (not `_target`).

## Issues — Spacedock
- `build.go` worktree-stage state-commit guidance shipped **unsubstituted** `{state_checkout}`/`{entity_path}` brace tokens (folded into k6, fixed). And relative `--workflow-dir` → relative `git -C` broke worktree-cwd commits (A0 fixed).
- The mod scanner reads the **state checkout** (`.spacedock-state/_mods/`), not `docs/dev/_mods/`, for split-root — non-obvious; surfaced by the pr-mod probe.
- **Dual-lineage repo**: `origin/main` = stale Python (0.12.1) with a no-branch-filter `pull_request_target` runtime-live-e2e that fires on every PR → red `static-offline` on next-PRs. Captain scoped it to `branches:[main]`.
- The benchmark-token at `~/.claude/benchmark-token` was **expired (401)**; refreshed by extracting the live OAuth token from the macOS keychain (`Claude Code-credentials`).
- Pre-existing env-only `TestCodexResolveManifestAgainstInstalledHost` fails under sandbox — noise on every `go test ./...`.

## Observations
_(captain to add)_ — Process wins: the parallel adversarial audit on a detached read-only checkout caught real bugs the stage validator missed (k6 binding, the live-test archived-path/heading) every cycle. next-as-source landing = cherry-pick entity code onto a branch off `origin/next` → PR → merge; **PR the entity branch BEFORE cleanup deletes it** (the friction pr-mod fixes).

## What's Next
- **Two gates parked for captain decision:** `ng` (VALIDATION-PASS + multi-agent fanout/synthesis) and `0m` `pr-merge-mod` — both ideation-complete, spike-grounded, gate-ready.
- **Phase B `sc`** gate-ready (state-remote already applied; orphan-branch model approved) — the state-init/sync half.
- **hx decomposition** `2a`/`se`/`5t` in backlog — ready to ideate; **A (`2a`) is the risky one** (the guard, with M1–M4 corrections).
- Other sessions' in-flight (not this FO's): `38` init-upgrade, `z0` cli-cobra-redesign, `7h` release-notes, `44` bundle-asset-distribution.
- Local `main` carries redundant merge commits (`172aa64` etc.) superseded by the PR squashes on `next`; harmless, can be reset/ignored.
