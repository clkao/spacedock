---
session-date: 2026-06-01
sequence: 2
first-commit: 4c3d380
last-commit: 9c55e27
duration: ~ full-day captain session (2026-05-31 → 06-01)
---

# Session Debrief — 2026-06-01 #2

This captain-session drove the install-story slice: hand-pushed the v0.19.0 cask, graduated `n1` → released **v0.19.1** (after rolling back a premature v0.20.0), ran the 0.19.2 set (38 / cli-cobra / release-notes) through to validation, stood up the pr-merge mod, and PR-shipped 38 onto `next`. (Runs concurrently with another session on the same shared `.spacedock-state`; #1 today was that session's.)

## Shipped
- **38** `init-upgrade-and-contract-remedy` — [#240](https://github.com/spacedock-dev/spacedock/pull/240). Stale-plugin dead-end → actionable `plugin-predates-contract` verdict + `init` 3-command upgrade (`marketplace add → uninstall → install`). Merged to `next`, done, archived.
- **n1** `graduate-plugin-onto-next` — done earlier this session (direct merge → **v0.19.1**): root `marketplace.json` (url+ref:next) + `.codex-plugin` manifest + full 5-skill surface + version-stamp/calendar-bump CI + `init` devBranch=next.
- **Releases:** v0.19.0 cask **hand-pushed** to the tap (HOMEBREW_TAP_TOKEN unset on the CI run); **v0.19.1** released after rolling back a premature **v0.20.0** (cadence: stay 0.19.x until the captain flips the minor).

## Validated & ready — NOT yet shipped (for next session)
- **z0** `cli-cobra-redesign` — validation PASSED + captain-approved. Branch needs: rebase onto `next`-with-38 + the **`init`→`install` sweep** (38's remedy text says `init`; cli-cobra renames the command) → PR via the pr-merge mod → live-e2e test.
- **7h** `release-notes-local-summary` — validation PASSED + captain-approved (cycle-1 AC-3 seam bug fixed: notes land in the tag BODY via two `-m` flags, `%(contents:body)` → goreleaser `--release-notes`). PR via the mod after cli-cobra.
- 38 + these two are the **0.19.2** set; 38 has landed, the other two await PRs, then the **0.19.2 tag** via the new local `spacedock-release notes` flow.

## Implemented & validated (entity still in-flight)
- **0m** `pr-merge-mod` — the workflow's first merge-lifecycle mod (`.spacedock-state/_mods/pr-merge.md`): opens a code-branch PR `--base next` at terminal, records `pr:`, mod-blocks until the PR merges, then terminalizes + archives; AC-3 no-`gh` fallback to local `--no-ff`. **Validated live by the 38 pilot** (#240). Entity still at `implementation` status — needs terminalization next session.

## Filed (backlog)
- **44** `bundle-asset-distribution` — `//go:embed` the plugin + inject `--plugin-dir` for zero-config Claude install; spike proved `--plugin-dir` overrides an installed plugin (so it beats a stale 0.12.1). **Blocked on a codex path** (codex has no `--plugin-dir` equivalent).
- **f2** `mods-definition-dir-location` — mods belong with the workflow definition (`docs/dev/_mods/`); change `scanMods` entityDir→definitionDir + migrate the pr-merge mod (atomic, or the merge guard goes dark).
- **jx** `pr-mod-audit-link-orphan-branch` — the pr-mod audit link points at the code SHA; must reference the orphan branch (`/blob/spacedock-state/dev/{path}`) — currently 404s.

## Non-PR commits (workflow-only)
State transitions and scaffolding for this session that didn't flow through a PR:
- `0653a92` `feat(pr-merge-mod): add pr-merge merge-lifecycle mod (state-root-aware, base next)` + `9077116` report — the mod itself (lives in `.spacedock-state/_mods/`).
- `b7b43a4`/`e3c77fe`/`58f4127`/`a7035f9` — 38's pr-merge mod lifecycle: mod-block set → `pr=#240` → mod-block cleared → done (all via the mod hook + post-merge advancement).
- Pushed `.spacedock-state` → `origin spacedock-state/dev` (`2bea327..8bbcd1f`) — orphan-branch state was local-only; now synced for resume/2nd-host.
- (Score bumps `645485b`/`9c55e27` and seed `49c367d` were captain/other-session edits on the shared state.)

## Decisions
- **cobra** for the CLI redesign (weighed vs hand-rolled / ffcli / kong) — confirmed the right call despite the per-command LOC; **Option-2 passthrough** (task before `--`, host flags after via `ArgsLenAtDash`) makes the prompt structurally unswallowable; **`init`→`install`** rename; jargon-free grouped help; drop META.
- **Stay on 0.19.x** until the captain flips the minor — rolled back a premature v0.20.0 → v0.19.1 (delete release+tag, re-tag, re-release; cask/plugin.json/.codex re-stamped, verified coherent).
- **Mods live with the workflow definition** (filed `f2`).
- **PR-onto-`next` via the pr-merge mod**, `--base next`; the `offline` CI job (`go test`) is the auto gate, live-e2e is `workflow_dispatch`/label-gated.
- **State pushed to the orphan branch** `spacedock-state/dev` going forward (not local-only).
- **Bundle distribution** (`44`) is the eventual zero-config Claude lane but waits on a codex path; 0.19.2 ships via 38 + cli-cobra + release-notes first.

## Issues — Workflow
- **Two-session shared `.spacedock-state`** is a live coordination hazard: concurrent-writer near-miss on `cli-cobra`'s entity (status-set vs a fold-in commit), `index.lock` contention, divergent `main` vs `origin/next` (reconciled by **merge, not rebase**, after a rebase would have rewritten the other session's live commits). Worth an explicit two-session protocol (overlaps `sc`).

## Issues — Spacedock
- **`runtime-live-e2e` label doesn't actually trigger CI:** `pull_request:` has no `types:`, so `labeled` is not a trigger event — adding the `run-live-e2e` label fires no run (and `gh pr create --label` is racy at open). Fix: add `labeled` to `pull_request: types:`, or use `workflow_dispatch`. (not filed)
- **pr-mod audit-link** points at the code worktree SHA, not the orphan branch where the entity file lives → 404 (filed as `jx`).
- **Mod scanner reads the state checkout** (`.spacedock-state/_mods/`) not the definition dir in split-root — non-obvious; captain decided mods should live with the definition (filed `f2`). (Also noted in #1.)
- Pre-existing `TestCodexResolveManifestAgainstInstalledHost` sandbox-only failure — noise on every `go test ./...`. (Noted #1.)
- Go-version non-reproducibility: local 1.26 vs CI 1.22 → different binary checksums; hand-pushed casks must pin the *published* checksums, never a local rebuild.

## Observations
_(none recorded)_

## What's Next
1. **cli-cobra PR** — rebase onto `next` + the `init`→`install` sweep (small code commit → worker) → PR via the mod → `workflow_dispatch` the live-e2e (or land the `labeled`-in-types CI fix first so the label works).
2. **release-notes PR** → then the **0.19.2 tag** via the new local notes flow.
3. **Terminalize `pr-merge-mod`** (`0m`) — its deliverable (the mod) is live + validated.
4. Backlog: `mods-definition-dir-location` (`f2`), `pr-mod-audit-link-orphan-branch` (`jx`), `sc` Phase-B sync (the two-session protocol), `bundle` (`44`, pending codex path), `yaml` (`zj`, held on the Python-oracle retirement prereq).
