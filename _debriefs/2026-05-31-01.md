---
session-date: 2026-05-31
sequence: 1
session-number: 3
first-commit: 61489d4
last-commit: de5bb44
state-range: e65d18f..HEAD
main-range: 0f9d628..de5bb44
duration: ~7h
---

# Session Debrief — 2026-05-31 (session 3)

Autonomous sprint, session 3: shipped the **entire "Ship the Launcher" slice** — the captain reframed the goal mid-session to (1) replace all custom safehouse launches, (2) an install path off the `next` branch, (3) a skeleton behavior test + coverage matrix. **Nine entities landed** (all direct `--no-ff` merges; no PR flow): the contract-version backbone, native `spacedock claude`/`spacedock codex` launchers via safehouse, comprehensive launch-parity, the behavior-test footing, the module migration to `github.com/spacedock-dev/spacedock`, the brew/release pipeline, and the dev-lane self-hosting plugin. Cut **v0.19.0** (GitHub Release published; cask-push token-gated). Ran the captain-directed **two-persona antipattern review** over the merged result (verdict: CONCERNS — real findings, queued).

## Shipped
- **tq** `spacedock-packaging` — direct merge `61489d4`; PASSED. The contract-version backbone (`CONTRACT_VERSION`, `requires-contract`, five-verdict `doctor`, three-point gate); session-3 closed the 2 audit holes — the front-door gate hole (`gateHost` now inspects the verdict, not the exit code) + the codex resolver (`codex plugin list`, no `--json`).
- **e7** `claude-safehouse-launcher` — direct merge `cab8b48`; PASSED. `spacedock claude` launches the FO through safehouse via a shared `internal/safehouse` Present/Available/Wrap seam; AC-6 live smoke captain-run.
- **be** `codex-safehouse-launcher` — direct merge `d7aed79`; PASSED. `spacedock codex` via safehouse (codex's own sandbox bypassed; FO reached by skill-name prompt token, no `--agent`); AC-4 confirmed by the captain's real `ps`.
- **2y** `sandbox-flag-passthrough` (launch-parity) — direct merge `524419e`; PASSED. Comprehensive launcher parity: fence task-prompt, resume-family suppression, `--plugin-dir` gate-relax, `--safehouse`/`--safehouse-enable=`/`--safehouse-add-dirs=` — validated against every real `ps` line.
- **80** `behavior-test-skeleton-and-matrix` — direct merge `c43a5c3`; PASSED. `internal/ensigncycle` behavioral skeleton (anchored emit-form, real-mutation-verified) + the coverage matrix classifying behavioral vs prose-grep vs GAP.
- **rz** `module-path-migration-spacedock-dev` — direct merge `857b341`; PASSED. Mechanical `github.com/clkao/spacedock-v1` → `github.com/spacedock-dev/spacedock`; pushed to `next`.
- **rg** `homebrew-tap` — direct merge `8723e2f`; PASSED (superseded by jf's cask). Own-tap formula v0.19.0; `brew audit --strict` rc=0 (captain-run via the tapped formula).
- **5a** `fresh-install-journey` — direct merge `4140d9b`; PASSED. **Dev-lane self-hosting loop closed**: v1 vendors its own `.claude-plugin/plugin.json` + first-officer/ensign entry points so `--plugin-dir <repo>` loads v1's contract; `requires-contract` gate green; `init.go` marketplaceSource fixed; install-journey doc.
- **jf** `release-pipeline` — direct merge `009dbd5`; PASSED. goreleaser **homebrew_casks** (migrated off hard-deprecated `brews:`) + tag-triggered workflow; `var Version = "0.19.0"`; F9 closed (stamp proven by running the binary).

## Filed (backlog)
- **k6** `dispatch-nonworktree-state-guidance` — the native dispatch injects split-root state-commit guidance only for worktree stages (`build.go:302`), so ideation ensigns get none (friction #1 fix).
- **n1** `graduate-plugin-onto-next` — vendor the FULL publishable plugin surface onto `next` (all user skills + both host manifests + marketplace.json + `init @next`) — the released-lane completion (next-as-source).

## Non-PR commits (workflow-only)
Direct `--no-ff` merges on main (no PR flow in this bootstrap workflow); state transitions live in the `.spacedock-state` repo. Surfaced:
- Feedback cycles: `spacedock-packaging` validation→implementation (2 audit fixes); `claude-safehouse-launcher` cycle 1 (resume-family widening); `codex-safehouse-launcher` cycle 1 (captain default-launch-prompt); `release-pipeline` validation→implementation (brews→homebrew_casks).
- Friction/notes commits in `_sprint-notes.md`: the recalibrated goal, hx disposition, the FO dogfooding **friction log** (7 items), the launch-parity ps spec, the carried tq-audit polish.
- `de5bb44` chore: removed the superseded hand-authored brew Formula staging (the cask is goreleaser-generated to the tap).
- Release: `v0.19.0` tag pushed → GitHub Release published with darwin tarballs + checksums.

## Decisions
- **next-as-source** (captain): publish the released-lane plugin from `next` (v1's vendored manifest) — NO cross-repo `requires-contract` edit to `main` needed. Full-plugin-on-next is the graduation (`n1`).
- **Codex no-`.safehouse` → plain `codex`, no bypass** (option b): `--dangerously-bypass-approvals-and-sandbox` is safehouse-path-only; symmetric with claude's plain fallback.
- **Fence convention** for the task prompt (`spacedock claude … -- "task"`); vocabulary-free, host value-flags before the fence.
- **brews → homebrew_casks** (goreleaser `brews:` hard-deprecated at v2.16; `goreleaser check` exit 2 → 0 after migration).
- **Default launch prompt** = the captain's send-off: `"You totally got this. Take your time. I love you. And tell all subagents and team members you love them too. Engage."` (codex appends `Assume $spacedock:first-officer for the entire session.`).
- **Version 0.19.0**; **safehouse docs URL = `https://agent-safehouse.dev`**; **minimal vendored scope** (first-officer+ensign) for the dev lane.
- FO has the conn for approvals + merges; full adversarial-audit ceremony on high-stakes (launch contracts, distribution).

## Issues — Workflow
FO dogfooding friction (full detail in `_sprint-notes.md` → FRICTION / IMPROVEMENT log):
1. **State-commit confusion on non-worktree (ideation) dispatches** — root-caused: `internal/dispatch/build.go:302` gates the split-root `git -C {state_checkout}` guidance on `worktreePath != ""`, so ideation ensigns get none + the vendored ensign contract frames the split-root section as "Worktree Contract." Filed as `k6`.
2. Contract-gate bootstrap cliff (malformed-range without `requires-contract`) — addressed by `--plugin-dir` relax + D's vendored manifest.
3. Read-then-`status --set` staleness echo (large entity files); Grep-over-Read mitigates.
4. `dispatch build` JSON must be written to a file (backticks/`<>` break heredocs).
5. Worktree `go build -o ./spacedock` artifact blocks `git worktree remove` (needs audited `--force`).

## Issues — Spacedock (candidates; NOT filed — need captain approval)
From the end-of-sprint antipattern review (CONCERNS):
- **Tautological test**: `internal/contract/gate_test.go` re-implements FO prose in-test + asserts against itself; the real front-door gate is never exercised. `Compare()` is dead API existing only for it. (test-hardening)
- **Flagship dispatch test drives the retired Python oracle**: `skills/integration/dispatch_test.go` execs `python3 claude-team build` (AC-3/AC-4 test the killed path), not native `dispatch.Run`. (test-hardening)
- **Unsigned-binary Gatekeeper gap**: `.goreleaser.yaml` has no sign/notarize, `CGO_ENABLED=0` → brew-installed binary quarantined → `spacedock --version` may be Gatekeeper-blocked on a clean Mac (threatens the headline install journey). (release-pipeline)
- **Post-`--`-fence front-door flags still consumed** → breaks the `--` literal-task contract; a test enshrines it. (launcher)
- **Unrunnable / inconsistent FO-contract instructions**: `member_exists` (an internal Python fn, no subcommand); `claude-team` invoked 3 ways, not on PATH, one hardcoding the plugin-private path the no-plugin-path rule forbids (untested). (→ zs)
- **Contract prose drift**: codex `init` reports no verdict on the normal path; stale `~175 chars`; phantom `DISPATCH_FILE:` token; self-referential maintenance note loaded into agent context. (→ hx)
- **No workflow-side `requires-contract` + no status contract-hint** — a dispatched ensign on a drifted installed contract has no signal.
- Mutable package-global `devBranch` bypasses the injectable ops seam.

## Observations
- **Dual-review (validator + parallel adversarial audit) earned its keep again** — the audits caught what validators missed every high-stakes cycle: the front-door gate hole's regression test (tq), the `--resume`-family under-broad gap (A), the brews v2.16 hard-deprecation (jf — by *running* goreleaser), the cask url/sha cross-check (jf), the dependency-closure trace (D).
- **Ensigns flagged-not-faked under sandbox limits** — brew audit (read-only `/opt/homebrew`), goreleaser (disk-full); each honestly deferred captain-run proofs rather than fabricating, and the FO routed the real verification (captain ran `brew audit` rc=0; the audit env ran goreleaser).
- **The self-hosting loop is closed for the dev lane** (`--plugin-dir <repo>` runs v1's OWN contract) but NOT yet for the dispatched ensigns this session — they ran the INSTALLED 0.12.1 contract (no dev path ran v1's vendored skills until D). This is the deepest finding (friction #7).
- **The prose-grep antipattern is real and pervasive** — the matrix flagged 6 `skills/integration` tests; the antipattern review confirmed them + found a 7th (the in-test gate re-implementation). `internal/ensigncycle` is the model for porting them (anchored emit-form + a prose-grep negative control).
- The captain's "next-as-source" + the migration converged v1 (Go) and the marketplace repo onto `spacedock-dev/spacedock` (`next` = Go-native; `main` = legacy Python).

## What's Next

**Immediate captain actions (brew lane):**
- Set **`HOMEBREW_TAP_TOKEN`** (PAT with write to `spacedock-dev/homebrew-tap`) on `spacedock-dev/spacedock` → re-run the v0.19.0 release → cask auto-publishes. (GitHub Release + tarballs already published.)
- Decide **notarization** (the Gatekeeper gap) — notarize in the release job, OR a cask postflight `xattr -dr com.apple.quarantine`, OR document the workaround.
- Decide the antipattern **fix-now vs file** set (below).

**Antipattern review follow-ups (the FO recommended fix-now set):**
- Test-hardening: delete the tautological `gate_test.go` + dead `Compare()`; repoint `dispatch_test.go` at native `dispatch.Run`.
- Launcher fix: short-circuit the fenced arm so post-`--` tokens are verbatim (the `--` literal-task bug).
- Release-pipeline: the notarization/Gatekeeper fix (M5).
- Fold into parked entities: `member_exists` + claude-team-invocation inconsistency → **zs**; codex-init-verdict + prose drift → **hx**; `devBranch` global → small refactor.

**Filed this session, ready to dispatch:**
- **k6** `dispatch-nonworktree-state-guidance` (friction #1 fix — non-worktree split-root injection + de-frame the contract section).
- **n1** `graduate-plugin-onto-next` (released-lane completion — full plugin surface on `next` + `@next` pinning; folds the stale `release.yml` comment).

**Parked (pre-reframe queue, post-bootstrap):** `hx` (deliverable-contract-hardening), `zs` (claude-runtime-segregation), `xd` (cli-ergonomics), `ng` (roborev-validation-hook), `0x` (architecture-review-cleanups), `zj` (yaml-parser-migration).

**State sync:** main = `de5bb44`, `spacedock 0.19.0 (contract 1)`; `next` pushed to `spacedock-dev/spacedock@next`; tag `v0.19.0` published.
